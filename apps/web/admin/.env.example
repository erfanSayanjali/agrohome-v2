# Admin app — browser hits same origin; Next rewrites /api/v1 and /uploads to API.
# Dev URL: http://localhost:4001/admin
API_URL=http://localhost:3002
# Leave empty so the browser uses /api/v1 (rewritten) — same pattern as production behind the proxy.
NEXT_PUBLIC_API_URL=
